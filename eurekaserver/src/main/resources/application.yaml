server:
  port: 8761
spring:
  application:
    name: eurekaserver
eureka: # add this config to make sure eureka not registry to eureka itself
  client:
    register-with-eureka: false
    fetch-registry: false
